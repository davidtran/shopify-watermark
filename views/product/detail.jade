extends ../layout

block content
  .container
    .row
      .col-md-12
        h1 #{product.name}
    .row
      each image in images
        .col-md-3
          img(src="#{image.src}", class="img-responsive")